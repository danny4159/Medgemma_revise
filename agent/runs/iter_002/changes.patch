diff --git a/README.md b/README.md
index 339ec1a..3c7a95b 100644
--- a/README.md
+++ b/README.md
@@ -2,4 +2,7 @@
 
 오케스트레이터가 진행하는 연구 코드. 접근법마다 `approach/<id>` 브랜치를 쓴다.
 - `results/`: 실험 결과 (git 제외, 브랜치와 상관없이 유지)
+- `summarize_scores.py`: `legacy/eval_results/scores.json`을 과제별 한 표로 정리 (CPU, 표준 라이브러리만).
+  실행: `python research/summarize_scores.py --output research/results/scores_summary.md`
+  점수는 초기 평가 시점의 조건(bbox는 비공식 xyxy 0-1 형식, n=10 소표본)의 값이며 현재 공식 사용법의 최종 성능이 아니다.
 - 입력 데이터와 이전 수동 분석 기록은 `../legacy/`
