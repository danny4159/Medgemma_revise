{
  "type": "object",
  "additionalProperties": false,
  "required": ["claude_tier", "tier_reason", "plan_markdown"],
  "properties": {
    "claude_tier": {"type": "string", "enum": ["heavy", "light"]},
    "tier_reason": {"type": "string", "description": "claude_tier를 고른 이유 한 줄"},
    "plan_markdown": {"type": "string", "description": "전체 연구 계획 (마크다운)"}
  }
}
