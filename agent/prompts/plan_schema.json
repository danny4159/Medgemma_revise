{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "claude_tier",
    "tier_reason",
    "review_mode",
    "review_reason",
    "plan_markdown"
  ],
  "properties": {
    "claude_tier": {
      "type": "string",
      "enum": [
        "creative",
        "heavy",
        "light"
      ]
    },
    "tier_reason": {
      "type": "string",
      "description": "claude_tier를 고른 이유 한 줄"
    },
    "review_mode": {
      "type": "string",
      "enum": [
        "full",
        "skip"
      ],
      "description": "Claude 작업 후 GPT 리뷰를 할지"
    },
    "review_reason": {
      "type": "string",
      "description": "review_mode를 고른 이유 한 줄"
    },
    "plan_markdown": {
      "type": "string",
      "description": "전체 연구 계획 (마크다운)"
    }
  }
}
